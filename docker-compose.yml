services:
  shank:
    build: .
    ports:
      - "8088:8080"
    environment:
      - DATA_DIR=/srv/shank/data
      - POLL_INTERVAL=10
    volumes:
      - ./data:/srv/shank/data
    networks:
      - shank-net

networks:
  shank-net:
    driver: bridge
